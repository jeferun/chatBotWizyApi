$kind: http-request
method: POST
url: '{{baseUrl}}/chatbot/chat'
order: 1000
headers:
  - key: 'Content-Type'
    value: 'application/json'
body:
  type: json
  content: |-
    {
      "query": "I am looking for a phone"
    }
auth:
  type: noauth
